import { Typography } from '@material-ui/core';
import React, { Component } from 'react';

export default class HealthPrime extends Component {
	render() {
		return (
			<Typography variant='h4' style={{ textAlign: 'center' }}>
				Health Prime
			</Typography>
		);
	}
}
